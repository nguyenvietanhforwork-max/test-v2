[build]
builder = "DOCKERFILE"
dockerfilePath = "services/agents/Dockerfile"
buildContext = "services/agents"

[deploy]
startCommand = "uvicorn agents.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
